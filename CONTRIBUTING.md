# 贡献与维护

请先说明要解决的问题、复现条件和预期结果。案例问题应抽象成可复用机制，同时保留验证边界，避免将某个页面的坐标、字号、数据或业务规则写成全局默认。

## 修改范围

六个 Skill 必须保持配套；共享引用、工具与素材集中在 `skills/repo-to-frontend/`。对单个阶段的请求不要扩展成完整流水线。图片审核与独立审查的默认值、状态继承和版本失效机制须保持一致。

## 验证

- 改动实现后，在具备依赖的环境运行最相关测试：`npx vitest run <test-file> --reporter=dot`。使用全局测试 API 的旧测试还需 `--globals`。失败时不得宣称完成。
- 修改 Skill 内容后，使用 Codex skill-creator 随附的 `quick_validate.py` 检查六个入口；Windows 使用 Python UTF-8 模式。
- 检查 Markdown 相对引用、跨 Skill 路径、文件清单和 SHA256。安装或同步前阅读两端差异，保留有效的独立修改。
- 涉及视觉或浏览器行为时附对应实际证据。格式、测试和文件一致性仅证明各自范围，不能代替真实视觉验收。

提交描述应包含问题、最终行为、实际执行的验证及剩余限制。返修同时解释先前为何漏检、如何防止同类误通过。

## 发布内容

只提交可分发的 Skill、共享工具与资源、必要测试和使用文档。排除凭据、环境文件、个人会话、案例业务数据、依赖目录及测试缓存。第三方许可与来源记录必须随文件保留。公开证据中的机器绝对路径须处理为可移植路径，并说明处理方式，不改写原始验证结论。
