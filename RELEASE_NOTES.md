# 动态数据与状态图优化 · 2026-09-11

本次将已完成优化和最终验收的六套 Skill 更新至公开仓库，共 77 个包内文件。沿用 3.2 工作流标记，以本发布说明及 PACKAGE_MANIFEST 的内容哈希识别修订。

## 变更

- 新增正式数据适应规则：参考图中的样本值不固化为业务常量；明确单位、时间、计数、排序、零值、缺失值和负值的含义。
- 强化同页同服务的持续更新证据，核对本次请求、响应及所有受影响的表格和图形；纯本地筛选可复用已加载输入的来源证据。
- 状态图片段绑定真实页面、状态、完整截图和版本证据；reference 模式支持这种交付，不能据此宣称 strict 像素一致。
- 修复负面报告容器类型异常被归一为空、从而漏读失败的问题；保留合法省略的兼容性，新增反例。
- 完整字段语义同时核对审核要求与实际生图提示词，并覆盖合成状态图。图形语义必须准确，不能临时增加未约定的逐像素门槛；已约定标准仍须遵守。

## 发布验证

- 六套源与安装目录的 77 个文件逐 SHA256 一致，且匹配最终优化任务的冻结清单；12 次格式校验、96 条相对 Markdown 引用核对通过。
- 发布前重新执行图片合同、后端交付、交付汇总和状态图片段的 4 个定向 Vitest 文件，128 项通过。
- 模板历史 browser.json 沿用首次发布的 8 个机器 URL 脱敏，测量数据和第三方许可保留。PACKAGE_MANIFEST 同时记录原文件和发行文件哈希。

## 证据边界

优化任务完成了三个代表页面及六个状态的有限范围独立验收，包含实际图片修正、浏览器状态和同页源更新链。业务数据、案例截图、私人日志及会话记录不随本 Skill 包公开。该任务的案例证据不等于本次重新运行了完整前端流程，也不能推广为全部业务、任意数据、上游实时采集或像素完全一致。五情境规则实验与基线持平，未证明普遍质量提升。

图片审核与独立审查仍是两个默认关闭的可选开关；必要自验不因此取消。第三方许可及自有内容尚无统一许可证的说明保持不变。

---

# 首次公开发布 · 2026-09-11

本次发布六个配套 Skill，共75个包内文件，基于2026-09-10完成验收及安装同步的稳定版本。工作流标记为3.2，已包含图片功能闭环；尚在验证中的动态数据适应性改动不包含在此发布中。

## 本次重新核验

- 75个原始包文件均与稳定安装验收清单的SHA256一致，六个入口格式校验通过。
- 图片合同、交付收口及后端交付的3个定向Vitest测试文件，共47项通过。测试对应的工具及测试文件与稳定版本一致。
- 包内相对Markdown引用、跨Skill路径以及第三方资源许可和来源随包核对。
- `assets/template-fallback/verification/browser.json` 的8个机器绝对资源URL改为相对路径，保留原始测量结果，并新增说明字段。该文件是脱敏后的历史证据；其余包文件字节保持原样。`PACKAGE_MANIFEST.json` 同时记录原文件与发行文件SHA256。

## 验证边界

本次没有重新进行真实业务仓库联调、模型生图或完整页面复刻。稳定版本此前的图片合同情境属于合成演练；模板截图是有限的模板验证证据。格式检查、文件一致性及单元测试不能证明任意项目的视觉或功能全链路已经通过。

独立子代理审查、图片审核均为可选开关，新任务默认关闭；必要自验和后端产品图片合同核对仍然执行。等待用户批准、暂停和资源不足均不能标记为完成。
